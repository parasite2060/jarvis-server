/**
 * GetConfigResponse — placeholder DTO (Story 13.10.5).
 * Story 13.13 fills the actual config.yml shape.
 */
export class GetConfigResponse {
  deepDreamCron?: string;
  weeklyReviewCron?: string;
  autoMerge?: boolean;
}
