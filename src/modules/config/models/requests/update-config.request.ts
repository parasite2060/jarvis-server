/**
 * UpdateConfigRequest — placeholder DTO (Story 13.10.5).
 * Story 13.13 fills validators per `cron-parser` validation rules.
 */
export class UpdateConfigRequest {
  deepDreamCron?: string;
  weeklyReviewCron?: string;
}
